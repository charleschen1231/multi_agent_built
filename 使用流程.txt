# 步骤 1: 准备数据（已完成）
# data/dataset_raw.jsonl 已包含 10 个测试问题

# 步骤 2: 定义系统配置（已完成）
# examples/system_sft_config.json 已配置好三 Agent 架构

# 步骤 3: 执行蒸馏训练
cd F:\code2025\multi_agent_built
conda activate multi_agent_built

# 完整流程：数据生成 + 训练
python cli/run_sft.py `
  --spec examples/system_sft_config.json `
  --input data/dataset_raw.jsonl `
  --do_train `
  --lr 2e-5 `
  --batch_size 4 `
  --epochs 3

# 仅生成 Ground Truth（不训练）
python cli/run_sft.py `
  --spec examples/system_sft_config.json `
  --input data/dataset_raw.jsonl `
  --teacher_only

# 使用已有数据进行训练
python cli/run_sft.py `
  --spec examples/system_sft_config.json `
  --data_file data/rollouts/sft_data_*_swift.jsonl `
  --do_train
